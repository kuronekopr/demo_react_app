# 実装計画: AIチャット & Code Interpreter リアルタイムデモ画面の構築

ユーザーの指示によって生成された React コードをその場で実行・表示する「Code Interpreter」の体験を提供する、高度なデモ画面を構築します。

---

## 🎨 画面設計とレイアウト

1. **全体レイアウト (2カラム構成)**
   - 画面全体を Glassmorphism のコンテナで覆い、左側 25% (1/4) に「AI チャットウィンドウ」、右側 75% (3/4) に「Code Interpreter レンダラー（プレビュー ＆ コード表示）」を配した機能的なレイアウトを採用します。

2. **左側 (25%): AI チャットウィンドウ**
   - AI アシスタントとの対話を模した、美しく滑らかなチャットインターフェース。
   - プリセットの「プロンプト指示ボタン」（例：「ネオン風の分析グラフ」「プレミアムなログイン画面」「インタラクティブカード」など）を設置し、クリックするとチャットに対話が追加され、右側のプレビューがその場で動的に書き換わるインタラクティブな仕組み。
   - チャット入力欄と送信アニメーション。

3. **右側 (75%): Code Interpreter レンダラー**
   - **プレビュータブ**: 生成された React コンポーネントが実際に動作するリアルタイム表示エリア。
   - **コード表示タブ**: AI が生成した TypeScript/CSS コードを、シンタックスハイライト風の流麗なフォーマットで閲覧できるソースコード表示エリア。
   - **動作ステータス表示**: 「Analyzing... ➡️ Generating Code... ➡️ Running Interpreter... ➡️ Success」といった、コードがその場で実行されているかのような動的なコンパイルステータスアニメーション。

---

## 🛠️ 実装するコンポーネント

### 1. `src/components/ChatWindow.tsx` [NEW]
- チャット履歴の管理と入力エリア。
- チャット送信時の擬似的な「AI 思考中・生成中」のインジケーターアニメーション。
- デモを瞬時に動かせる「おすすめの指示（プロンプト）」のワンタップトリガー。

### 2. `src/components/CodeInterpreter.tsx` [NEW]
- プレビューとコード表示のタブ切り替え。
- 指示に基づいて動的にマウントされるコンポーネント（以下の3パターンを実装）：
  1. **Analytics Dashboard (ネオン分析グラフ)**: 流麗なCSSアニメーションを伴う3D風データチャート。
  2. **Premium Login (ログイン画面)**: 入力バリデーションやスムーズなフォーカスエフェクトを持つ美麗なフォーム。
  3. **Task Board (タスクボード)**: カンバン風のインタラクティブなステータス管理ボード。

### 3. `src/App.tsx` [MODIFY]
- ダッシュボードUIから、今回の「Chat & Interpreter」UIへの全面的なレイアウト変更。
- チャットの入力や指示のアクションに応じて、右側の Code Interpreter に渡す「ソースコード文字列」と「レンダリング対象コンポーネント」を動的に変化させるステート連携の実装。

### 4. `src/index.css` [MODIFY]
- 新しい 2カラムレイアウト用の CSS 設計。
- チャットバブル、ソースコード表示用のエディタ風背景、およびコンパイルインジケーター用のアニメーションの追加。

---

## 🧪 検証プラン

### 1. 自動テスト / 動作確認
- `npm run build` がエラーなく通り、TypeScript のコンパイルが正常に行われること。

### 2. 手動確認
- 開発サーバー (`npm run dev`) を起動し、以下の操作性を確認します：
  - 左側のプロンプトボタンをクリックした際、AIの返答チャットが表示され、右側の「コンパイルアニメーション」が走り、プレビューが即座に切り替わること。
  - 「Preview」と「Code」のタブ切り替えが正しく機能し、表示されるソースコードがシンタックスハイライトされていること。
  - 生成された各プレビューコンポーネント（グラフ、ログイン、ボード）が実際にクリックやホバーでインタラクティブに動作すること。
